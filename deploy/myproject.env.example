# Copy to /etc/myproject/myproject.env, replace the placeholders, and keep the
# deployed file readable only by root. Do not commit production credentials.
MYSQL_HOST=127.0.0.1
MYSQL_USER=site_user
MYSQL_PASSWORD=change-me
MYPROJECT_LOG_FILE=/srv/digitalOcean/logs/myproject.log
RATELIMIT_STORAGE_URI=redis://127.0.0.1:6379/0
